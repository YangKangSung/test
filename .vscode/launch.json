{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Python: Current File (File-Folder venv)",
			"type": "python",
			"request": "launch",
			"program": "${file}",
			"console": "integratedTerminal",
			// 디버깅 시 ${fileDirname} 하위의 venv 사용
			"python": "${fileDirname}/.venv/Scripts/python.exe" // :contentReference[oaicite:9]{index=9}
		}
	]
}
